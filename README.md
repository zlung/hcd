# hcd
easy cd to your long path directory

## Installation

```shell
$ git clone https://github.com/zlung/hcd
$ cd hcd
$ ./install.sh
```

*if ~/bin not in your $PATH:*
```shell
$ source ~/.bashrc
```
